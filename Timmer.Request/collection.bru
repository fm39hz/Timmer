auth {
  mode: bearer
}

vars:pre-request {
  hostName: http://localhost:5062
  version: api/v1
}

docs {
  # Authentication
  - Use login method to generate accessToken and refreshToken
  - Set authorization for request
  
}
